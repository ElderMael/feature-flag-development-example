spring:
  application:
    name: "api"
  cloud:
    kubernetes:
      client:
        namespace: ff
      config:
        enabled: true
        enableApi: true
        name: api
        namespace: ff
